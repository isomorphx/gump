version: 2

project_name: gump

builds:
  - id: gump
    main: .
    binary: gump
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X {{.ModulePath}}/internal/version.Version={{.Version}}
      - -X {{.ModulePath}}/internal/version.Commit={{.ShortCommit}}
      - -X {{.ModulePath}}/internal/version.BuildDate={{.Date}}

archives:
  - id: default
    format: tar.gz
    name_template: "gump_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

release:
  github:
    owner: isomorphx
    name: gump
  draft: false
  prerelease: auto
  extra_files:
    - glob: ./install.sh

brews:
  - name: gump
    repository:
      owner: isomorphx
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://gump.build"
    description: "Orchestrate AI coding agents with declarative recipes"
    license: "MIT"
    install: |
      bin.install "gump"
    test: |
      system "#{bin}/gump", "--version"

