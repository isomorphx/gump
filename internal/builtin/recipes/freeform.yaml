name: freeform
description: Single agent, no plan. For testing and quick experiments.
steps:
  - name: execute
    agent: claude-opus
    output: diff
    guard:
      max_turns: 80
    prompt: "{spec}"
    gate: [compile]
