name: test
description: Minimal single-step diff recipe for report metrics (M5)
max_budget: 5.00
steps:
  - name: impl
    agent: claude-opus
    output: diff
    prompt: |
      Implement the spec.
    gate: [compile]
